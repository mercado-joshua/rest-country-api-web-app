// Wait for document to load

document.addEventListener('DOMContentLoaded', event => {
    // adds data-theme="dark" in the root element/ <html> tag
    document.documentElement.setAttribute('data-theme', 'light');
});